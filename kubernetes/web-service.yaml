apiVersion: v1
kind: Service
metadata:
    name: Modul300_LB02-web
    labels:
        app: Modul300_LB02-web
        tier: frontend
spec:
    type: LoadBalancer
    ports:
        - port: 80
    selector:
        app: Modul300_LB02-web
        tier: frontend