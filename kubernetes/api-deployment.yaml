apiVersion: extensions/v1beta1
kind: Deployment
metadata:
    name: modul300-lb02-api
spec:
    replicas: 1
    template:
        metadata:
            labels:
                app: modul300-lb02-api
                tier: backend
        spec:
            containers:
                - name: modul300-lb02-api
                  image: kalessandra/modul300_lb02-api:latest
                  resources:
                      requests:
                          cpu: 100m
                          memory: 100Mi
                  ports:
                      - containerPort: 8080